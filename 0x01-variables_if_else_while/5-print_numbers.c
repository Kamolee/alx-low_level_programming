#include <stdio.h>
/**
 * Main- program that prints all single digit numbers
 * of base 10 starting from 0
 *
 * Return:0
 */
int Main(void)
{ int num;
for (num = 0; num < 10; num++)
printf("%d", num);
printf("\n");
}
