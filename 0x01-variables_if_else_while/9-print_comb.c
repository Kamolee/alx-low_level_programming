#include<stdio>
/**
 * main- program that prints all possible combinations of single-digit numbers
 * Return: 0
 */
int main(void)
{
int m;
for (m = 48; n < 58; m++)
{putchar (m);
if (m != 57)
{putchar (',');
putchar ('');
}
putchar('\n');
return (0);
}
